# JARVIS — Dipendenze
# Installa con: pip install -r requirements.txt

# ── Core async HTTP (per Ollama API) ──────────────────────────────────────
httpx>=0.27.0
aiofiles>=23.2.0

# ── Database ─────────────────────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Audio I/O (NO PyAudio nativo WASAPI) ────────────────────────────────
# sounddevice usa PortAudio via ctypes → stabile su Windows, Linux, macOS.
# Non richiede PyAudio e non ha i problemi WASAPI.
sounddevice>=0.4.7
numpy>=1.26.0

# ── STT (Speech-to-Text) ─────────────────────────────────────────────────
# SpeechRecognition usa Google STT via HTTP (no API key per il free tier).
SpeechRecognition>=3.10.0

# ── TTS (Text-to-Speech) ─────────────────────────────────────────────────
# pyttsx3 usa le voci native del SO: SAPI5 su Windows, espeak su Linux,
# NSSpeechSynthesizer su macOS. Offline, zero latenza di rete.
pyttsx3>=2.90

# ── System monitoring (opzionale, per system_info tool) ───────────────────
# psutil>=5.9.0

# ── Vector memory (opzionale) ─────────────────────────────────────────────
# sentence-transformers>=2.7.0
# faiss-cpu>=1.8.0
# chromadb>=0.5.0

# ── Dev / Test ────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0